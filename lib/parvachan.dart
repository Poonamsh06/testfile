import 'package:flutter/material.dart';
class ParvachanPage extends StatelessWidget {
  const ParvachanPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Hello")),
    );
  }
}
